#include "Timer.h"
#pragma comment(lib,"winmm.lib")
#include<windows.h>
#include<wchar.h>

Timer::Timer(float nowTime)
	:nowTime(nowTime)
{
}

float Timer::GetTime()
{
	DWORD Before, Affter;



	return nowTime;
}
